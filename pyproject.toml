[project]
name = "bayesian-regime-detection-engine"
version = "0.1.0"
description = "Project 1A Bayesian Regime Detection Engine for Indian equity direction forecasting."
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
